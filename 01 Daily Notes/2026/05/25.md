Ника, вот полноценная запись на **25 мая 2026 года**. Копируй и вставляй в свою ежедневную заметку в Obsidian.

---

## 📅 Daily Note — 2026-05-25

### 🎯 Focus of the day
> **Запуск домена `syverro.com` и доведение до конца всех DNS- и деплой-задач.**

---

## 🤖 Сессия с GPT / DeepSeek

### ✅ Что было сделано

- [x] Диагностирована проблема: при открытии `syverro.com` показывался `index.js`, а не лендинг
- [x] Установлена причина: `vercel --prod` выполнялся из корня проекта, а не из папки `syverro-landing`
- [x] Выполнен правильный деплой: `cd syverro-landing && vercel --prod`
- [x] Обнаружено, что домен в Nic.ru был делегирован на «Статусная страница», а не на «DNS-master»
- [x] В карточке домена → «Управление DNS-серверами» → выбран только `DNS-master`
- [x] Подтверждено, что A-запись (`@ → 216.198.79.1`) и CNAME-запись (`www → cname.vercel-dns.com.`) добавлены и выгружены
- [x] Дождались DNS-пропагации (≈2–3 часа)
- [x] SSL-сертификат от Vercel выдался автоматически
- [x] Итог: `syverro.com` открывает лендинг, работает HTTPS

### 🐛 Проблемы, с которыми столкнулись

| Проблема | Решение |
|----------|---------|
| Vercel выложил `index.js` вместо HTML | Выполнять `vercel --prod` **только из папки** `syverro-landing` |
| Домен не принимал A/CNAME записи | В Nic.ru переключить DNS-серверы со `statuspage` на `DNS-master` |
| `ns3-l2.nic.ru` и другие не работали с кастомными записями | Это стандартные серверы Ru-Center. Проблема была в выборе `DNS-master`, а не в них |
| Долгое ожидание изменений | DNS-пропагация заняла около 2–3 часов (норма) |

### 🧠 Главные выводы

- **Деплой на Vercel** должен происходить из той папки, где лежит `index.html`, а не из корня React Native-проекта
- **Nic.ru** требует **ручного переключения на `DNS-master`** для работы с A/CNAME записями (по умолчанию там статусная страница)
- **DNS-изменения не мгновенны** — всегда нужно закладывать время на пропагацию (TTL, кеширование)
- **Vercel сам выдаёт SSL-сертификат** — не нужно ничего настраивать дополнительно

---

## ⚡ Dev log

### Ночь
- 🔄 Анализ проблемы: `syverro.com` показывает `index.js`
- 🔍 Проверка: `vercel --prod` выполнялся из корня, а не из `syverro-landing`
- ✅ Передеплой из правильной папки
- 🔍 Проверка DNS-статуса в Nic.ru: домен делегирован на `statuspage`
- ✅ Переключение на `DNS-master` в настройках домена
- ✅ Добавление A и CNAME записей
- ✅ Выгрузка DNS-зоны
- ⏱️ Ожидание DNS-пропагации (≈2–3 часа)
- ✅ Проверка: `syverro.com` открывает лендинг, работает HTTPS
- ✅ SSL-сертификат от Vercel активен

---

## 🔗 Связанные заметки

- [[2026-05-24]] — предыдущий день (домен куплен, первый деплой)
- [[Лендинг Syverro]] — ссылка на структуру лендинга
- [[DNS-настройка]] — шпаргалка по записям A, CNAME, NS

---

## 🎯 Завтрашний фокус (26 мая)

- [ ] Подключить почтовое перенаправление `hello@syverro.com` на личную почту
- [ ] Собрать обратную связь от тестовых пользователей (если есть)
- [ ] Вернуться к разработке приложения (челленджи, ачивки, обложки)

---

## 🦊 Итог дня

**Домен `syverro.com` запущен и работает.** Лендинг доступен по HTTPS, SSL-сертификат активен, DNS-записи настроены правильно. Техническая инфраструктура для публичного присутствия Syverro готова.
